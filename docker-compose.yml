services:
  erepmax:
    build: .
    container_name: erepmax
    restart: unless-stopped
    ports:
      - "6868:3000"
    volumes:
      - /mnt/user/appdata/erepmax/data:/data
    environment:
      - DB_PATH=/data/erepmax.db
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
